//                             using let keyword 

let firstName = 'mohammed' // camel case
let last_Name = 'khaled' //  snake case
let age = '22'


let userIntro = 'my name is ' + firstName + ' ' + last_Name + ' ' + 'i am ' + ' ' + age + ' ' + 'year old '






//                            using const keyword

// we can not change variable value 
// we use const if we dont want to change the variable value


const hoursInDay = 24
 
hoursInDay = 18   // it will not change value it shows error



//                             using var keyword

var a = 200
{ var a = 300 }//block

{
    let b = 300
}





//    var and let are same but its have some different like

//   1. var can be access out of the block but let can not be access out of the block
//   2. we can use var before declare the variable its shows undefine if we use let before declaring the variable its shows error




